# 🚀 GitHub Pages 部署指南

## 快速部署到 GitHub Pages

### 🎯 一键设置

运行快速设置脚本：

```bash
node scripts/setup-deployment.js
```

这个脚本会：
- ✅ 检查必要文件
- ✅ 生成加密密钥
- ✅ 测试配置加密
- ✅ 提供部署说明

### 📋 手动部署步骤

#### 1. 启用 GitHub Pages

- 进入仓库 **Settings** → **Pages**
- Source 选择 **GitHub Actions**

#### 2. 设置加密密钥

- 进入 **Settings** → **Secrets and variables** → **Actions**
- 添加 Secret：
  - Name: `CONFIG_ENCRYPTION_KEY`
  - Value: `4d9e73d8266266dd02f2d1f80c3c8e37bd9cc84068e6b7ddc810ad51a52abfbf`

#### 3. 准备配置模板

确保 `config/setting-template.json` 存在：

```json
{
  "weather": {
    "app_id": "你的天气API_ID",
    "app_secret": "你的天气API密钥"
  },
  "其他配置": "..."
}
```

#### 4. 推送部署

```bash
git add .
git commit -m "配置GitHub Pages部署"
git push origin main
```

### 🔧 工作流程

推送代码到 `main` 分支后：

1. 🏗️ **自动构建**
   - 检出代码
   - 加密敏感配置
   - 生成 JSON & JS 配置文件

2. ✅ **验证文件**
   - JSON 格式检查
   - JavaScript 语法检查

3. 🚀 **自动部署**
   - 部署到 GitHub Pages
   - 绑定域名 `miate.top`

### 📁 文件说明

- `deploy-secure.yml` - 安全部署工作流（推荐）
- `static.yml` - 简单部署工作流
- `encrypt-config.js` - 配置加密工具
- `config-loader.js` - 前端配置加载器

### 🔍 监控部署

- 查看仓库 **Actions** 选项卡
- 点击 "Deploy with encrypted config" 查看详细日志
- 部署完成后访问 `https://miate.top`

### ⚠️ 注意事项

- ✅ 敏感信息只在 `setting-template.json` 中明文存储
- ✅ 生产环境使用加密后的配置文件
- ✅ 不要在代码中硬编码任何密钥
- ✅ 定期轮换加密密钥

### 🆘 常见问题

**Q: 部署失败怎么办？**
A: 检查 Actions 日志，确认 GitHub Secrets 设置正确

**Q: 如何更新配置？**
A: 修改 `setting-template.json`，推送后自动重新加密部署

**Q: 支持哪些配置格式？**
A: 同时生成 JSON 和 JavaScript 两种格式

---

详细文档请查看：[GitHub 部署完整指南](docs/github-deployment.md)